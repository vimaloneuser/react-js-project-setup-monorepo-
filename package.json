{
  "name": "root",
  "private": true,
  "version": "1.0.0",
  "devDependencies": {
    "lerna": "^4.0.0"
  },
  "scripts": {
    "start": "lerna run start --scope={web-client,server-api}",
    "test":"lerna run test --scope=web-client"
  }
}
